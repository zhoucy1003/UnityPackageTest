using UnityEngine;

namespace unity_package_test
{
    public class ShowStringInEditor : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {
            Debug.Log("Test In Editor");
        }
    }
}

